interface:
  display_name: "Authio Node"
  short_description: "Repo-specific patterns and workflows for authio_node"
  default_prompt: "Use the authio_node repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true