# Tenant id in *your* SaaS (demo uses a fixed value)
DEMO_TENANT_ID=tenant_demo_acme

# 32+ byte secret for AES-256-GCM — generate with: openssl rand -base64 32
INTEGRATOR_CREDS_KEY=

# Optional — Management API base (defaults to https://api.authio.com)
AUTHIO_API_URL=https://api.authio.com
